service:
  type: LoadBalancer
  loadBalancerIP:
ingress:
  enabled: true
  hosts:
    - host: nextcloud.localhost.com
      paths:
        - /
nextcloud:
  host: nextcloud.localhost.com

  # user (username, password) = (admin, changme) by default

internalDatabase:
  enabled: true

# externalDatabase:
#   enabled: false
#   type: postgresql
#   database: nextcloud_db
#   # 5432 is default port for postgresql
#   host: nextcloud_db.nextcloud.svc.cluster.local:5432
#
#   user: nextcloud
#   password: nextcloud_testing
#
# postgresql:
#   enabled: true
#   global:
#     postgresql:
#       auth:
#         username: nextcloud
#         password: nextcloud_testing
#         database: nextcloud_db
